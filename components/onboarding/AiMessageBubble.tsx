import { AiOnboardingMessage } from "@/components/onboarding/AiOnboardingMessage";

export function AiMessageBubble() {
  return (
    <AiOnboardingMessage
      headline="What's happening next?"
      supportingText="Tell us what you're already doing so we can turn it into a fan experience."
    />
  );
}
